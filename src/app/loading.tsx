import React from 'react';
import CommonSkeleton from "@/components/shared/skeleton/CommonSkeleton";

const Loading = () => {
    return (
        <div>
         <CommonSkeleton/>
        </div>
    );
};

export default Loading;